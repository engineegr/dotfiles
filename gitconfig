[user]
	name = Gonzo-soc
	email = gonzo.soc@gmail.com
[core]
	editor = vim
	autocrlf = input
[color]
	ui = auto
[receive]
	fsckObjects = true
[alias]
	co = checkout
	br = branch
	cm = commit
	st = status
	unstage = reset HEAD --
	lastcm = log -1 HEAD
  grog = log --graph --abbrev-commit --decorate --all --format=format:\"%C(bold blue)%h%C(reset) - %C(bold cyan)%aD%C(dim white) - %an%C(reset) %C(bold green)(%ar)%C(reset)%C(bold yellow)%d%C(reset)%n %C(white)%s%C(reset)\"

[push]
	default = matching
